#!/bin/zsh

CLIP="cat"

which xclip > /dev/null
if [[ $? -eq 0 ]] then CLIP="xclip"; fi

which pbcopy > /dev/null
if [[ $? -eq 0 ]] then CLIP="pbcopy"; fi


$HOME/etc/lib/supergenpass/core/sgp -q -l 16 $1 | $CLIP
